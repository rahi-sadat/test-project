#include<bits/stdc++.h>
using namespace std;
#define ll long long int 

void RUHAN(){
 ll n;   cin>>n;
vector<ll>v(n);
 for(int i=0;i<n;i++)
{
cin>>v[i];
}
    for(auto x:v)  cout<<x<<" ";  
    cout<<endl; 
    //// this is the temp change 
}
int main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
    long long t;
    cin>>t;
    while(t--) RUHAN();
}